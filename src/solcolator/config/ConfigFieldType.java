package solcolator.config;

/**
 * An enum specifying the supported types of fields in the configuration
 * 
 *
 */
public enum ConfigFieldType {
	STRING, BOOLEAN, INT, LONG, FLOAT, DOUBLE, ARRAY, NAMED_LIST, NAMED_LIST_AS_CONFIG
}